import 'package:flutter/material.dart';
class LogoMainLogo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Container(
          width: 206.10,
          height: 99.82,
          clipBehavior: Clip.antiAlias,
          decoration: BoxDecoration(),
          child: Stack(),
        ),
      ],
    );
  }
}